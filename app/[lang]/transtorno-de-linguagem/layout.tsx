import type { Metadata } from "next"

export const metadata: Metadata = { title: "Transtorno do Desenvolvimento da Linguagem", description: "Tempo de processamento, vocabulário explícito e compreensão leitora no TDL." }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
